rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // The app uploads images via the backend API (Cloud Functions),
    // not directly from the client.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

